{
  "name": "open-bug-reporter-functions",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions:submitBugReport"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "firebase-functions": "^5.1.1",
    "resend": "^4.0.0"
  }
}
